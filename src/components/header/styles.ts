import styled from "styled-components";

export const Header = styled.div`
    width: 100%;
    position: absolute;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 45px;
    padding: 0 40px;
`